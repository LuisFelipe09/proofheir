{
  "name": "contracts",
  "root": "packages/contracts",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "forge build",
        "cwd": "packages/contracts"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "forge test",
        "cwd": "packages/contracts"
      }
    }
  }
}
