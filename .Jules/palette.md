## 2024-01-02 - Form Accessibility Pattern
**Learning:** The application frequently uses inline styles for layout, which seems to correlate with a lack of semantic HTML structure (specifically missing `id` and `htmlFor` associations).
**Action:** When working on components with inline styles, double-check that form inputs have explicit `id`s and that labels use `htmlFor` to reference them.
